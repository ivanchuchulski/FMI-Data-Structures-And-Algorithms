#include <iostream>
#include <cstdlib>
#include <cstdio>

int main() {

	/*
	
		Singly linked				no tail		with tail
	push_front						O(1)		
	top_front						O(1)
	pop_front						O(1)
	push_back(key)					O(n)		O(1)
	top_back()						O(n)		O(1)
	find(key)						O(n)
	erase(key)						O(n)
	is_entry						O(1)
	add_before(Node, key)			O(n)
	add_after(Node, key)			O(1)
	
		Doubly linked					no tail		with tail
	push_front						O(1)		
	top_front						O(1)
	pop_front						O(1)
	push_back(key)					O(n)		O(1)
	top_back()						O(n)		O(1)
	find(key)						O(n)		O(1)
	erase(key)						O(n)
	is_entry						O(1)
	add_before(Node, key)			O(1)
	add_after(Node, key)			O(1)	
	*/

	std::cout << (20 ^ 110) << '\n';

	std::printf("%s", '\n');





	std::system("pause");
	return 0;
}